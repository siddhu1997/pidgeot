import { PlaygroundLab } from "@/components/playground/playground-lab";

export const metadata = {
  title: "Playground | Pidgeot",
  description: "Phase 6A visual laboratory for deterministic, isolated Pidgeot UI experiments.",
};

export default function PlaygroundPage() {
  return <PlaygroundLab />;
}